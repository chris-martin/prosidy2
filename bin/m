#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
export NIX_PATH="nixpkgs=$PWD/nix/nixpkgs.nix"

nix run --file nix/default.nix m --command m "$@" || {
    nix log --file nix/default.nix m | cat
    exit 1
}
